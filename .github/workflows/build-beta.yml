---
name: Build atomic (Beta)
on: # yamllint disable-line rule:truthy rule:comments
  # schedule:
  #   # yamllint disable-line rule:comments
  #   - cron: "41 6 * * 0" # 6:41 UTC Sunday
  #   # yamllint disable-line rule:comments
  #   - cron: "41 6 * * 1,2,3,4,5,6" # 6:41 UTC Monday-Saturday
  workflow_dispatch:
    inputs:
      build_type:
        description: "Build Type"
        required: true
        default: "image"
        type: choice
        options:
          - image
          - iso
          - both

jobs:
  lint:
    name: Lint Project
    permissions:
      contents: read
    uses: ./.github/workflows/lint.yml

  build-image:
    name: Build Images
    needs: [lint]
    if: |
      (github.event_name == 'schedule') ||
      (github.event_name == 'workflow_dispatch' &&
        (inputs.build_type == 'image' || inputs.build_type == 'both'))
    uses: ./.github/workflows/build-image.yml
    permissions:
      actions: read
      contents: read
      packages: write
    secrets:
      SIGNING_SECRET: ${{ secrets.SIGNING_SECRET }}
    with:
      image_flavor: Beta

  build-iso:
    name: Build ISOs
    if: |
      (inputs.build_type == 'iso' || inputs.build_type == 'both') &&
      !contains(github.head_ref, 'renovate')
    uses: ./.github/workflows/build-iso.yml
    permissions:
      actions: read
      contents: read
      packages: read
    needs: [build-image]
    with:
      image_flavor: Beta

  gen-sbom:
    name: Generate SBOMs
    if: |
      !contains(github.head_ref, 'renovate') &&
      (
        (github.event_name == 'schedule') ||
        (github.event_name == 'workflow_dispatch' &&
          (inputs.build_type == 'image' || inputs.build_type == 'both'))
      )
    needs: [build-image]
    uses: ./.github/workflows/gen-sbom.yml
    permissions:
      actions: read
      contents: read
      packages: write
      security-events: write
    secrets:
      SIGNING_SECRET: ${{ secrets.SIGNING_SECRET }}
    with:
      image_flavor: Beta

  create-release:
    name: Create Release
    if: |
      (inputs.build_type == 'iso' || inputs.build_type == 'both') &&
      !contains(github.head_ref, 'renovate')
    needs: ["build-iso", "gen-sbom"]
    uses: ./.github/workflows/create-release.yml
    permissions:
      contents: write
    with:
      type: beta
